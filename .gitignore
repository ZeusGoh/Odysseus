# built output — regenerate with: python tools/build_single.py
dist/

# editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# node, if tooling ever needs it
node_modules/

# credentials and personal notes — never commit these
login.txt
.env
*.key

# analyst reports are generated output; the example is the schema reference
verdicts/*.json
!verdicts/example.json
verdicts/watch.log
verdicts/.watch-state.json
verdicts/.track-record-stoch.json
verdicts/.track-record-news.json
verdicts/.track-record-crowd.json
verdicts/.bybit-key.json
verdicts/.last-pass.json
verdicts/.chat.json
verdicts/.watch-lock.json

# generated at runtime on this machine — logs, the desk brief, and Claude Code local settings
verdicts/*.log
verdicts/.desk-brief.md
.claude/settings.local.json
